<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>BRODemy</title>
    <script src="js/jquery-2.2.1.js"></script>
    <script type="text/javascript" src="js/materialize.min.js"></script>
    <script src="js/select.js"></script>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <!-- bootstrap - link cdn -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css"
          integrity="sha384-1q8mTJOASx8j1Au+a5WDVnPi2lkFfwwEAa8hDDdjZlpLegxhjVME1fgjWPGmkzs7" crossorigin="anonymous">
    <!-- carregando icones -->
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <!-- incluindo css -->
    <link href="css/style.css" rel="stylesheet">
    <!-- materialize -->
    <link type="text/css" rel="stylesheet" href="css/materialize.css" media="screen,projection"/>
    <link rel="icon" href="./images/bro_icon.png">
</head>
<body>

<div class="container">
    <form action="/register_course" method="post">
        <div class="jumbotron center-align" id="formulario">
            <div class="col-md-12">
                <div class="row">
                    <div class="input-field col s12">
                        <i class="material-icons prefix">account_circle</i>
                        <input id="title" name="title" type="text" value="<%= course.title %>" class="validate">
                        <label class="active" for="title">Title</label>
                    </div>
                </div>
                <div class="row">
                    <div class="input-field col s12">
                        <i class="material-icons prefix">vpn_key</i>
                        <input id="description" name="description" type="text" value="<%= course.description %>"
                               class="validate">
                        <label class="active" for="description">description</label>
                    </div>
                </div>
                <div class="row">
                    <div class="input-field col s12">
                        <i class="material-icons prefix">vpn_key</i>
                        <input id="requisites" name="requisites" type="text" value="<%= course.requisites %>"
                               class="validate">
                        <label class="active" for="requisites">requisites</label>
                    </div>
                </div>
                <div class="input-field col s12">
                    <select name="level">
                        <option value="easy" selected>easy</option>
                        <option value="middle">middle</option>
                        <option value="hard">hard</option>
                    </select>
                    <label>Level of Course</label>
                </div>
                <div class="row">
                    <div class="input-field col s12">
                        <i class="material-icons prefix">email</i>
                        <input id="image" name="image" type="text" value="<%= course.image %>" class="validate">
                        <label class="active" for="image">link of image</label>
                    </div>
                </div>
                <input type="hidden" name="_id" value="<%= user._id %>">
            </div>
            <button type="submit" class="btn btn-default">Cadastrar</button>
        </div>
    </form>
</div>
<script type="text/javascript" src="js/materialize.min.js"></script>
</body>
</html>
