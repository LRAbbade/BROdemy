<!DOCTYPE html>
<html lang="en">
<head>
    <title>Mudar Senha</title>
    <link rel="stylesheet" type="text/css" href="./css/login_style.css">
    <link rel="icon" href="./images/bro_icon.png">
</head>
<body>
<%- include ../navbar.ejs %>

<% if(validacao.length > 0){ %>
<div class="info-box">
    <p id="no-courses-error"><strong><%= validacao[0].msg %></strong></p>
</div>
<% } %>

<div class="info-box">
    <form action="/change_password" method="post">
        <div class="row">
            <i class="material-icons prefix">account_circle</i>
            <label>Antiga Senha
                <input class="text-input-single-line" name="passwordBefore"
                       type="password" class="validate" required>
            </label>
        </div>
        <div class="row">
            <i class="material-icons prefix">vpn_key</i>
            <label>Nova Senha
                <input class="text-input-single-line" name="passwordAfter"
                       type="password" class="validate" required>
            </label>
        </div>
        <div class="row">
            <i class="material-icons prefix">vpn_key</i>
            <label>Repetir a Senha
                <input class="text-input-single-line" name="passwordAfter2"
                       type="password" class="validate" required>
            </label>
        </div>
        <div class="row">
            <button type="submit" class="std-btn">Mudar a Senha</button>
        </div>
    </form>
</div>

</body>
</html>
